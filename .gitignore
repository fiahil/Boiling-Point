/target
**/*.rs.bk
Cargo.lock.orig
.DS_Store
# archived agent-harness Node dependencies (guards a revival `npm install`)
/archive/agent-harness/node_modules/
# legacy playtest logs (the script is archived; local clones may still have these)
/.playtest/
